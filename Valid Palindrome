/**
 * @param {string} s
 * @return {boolean}
 */
var isPalindrome = function(s) {
   s= s.replace(/_|\W/g,"").toLowerCase();
    var i = 0; 
    var j = s.length-1;
    while(i<=j){
        if(s[i]!==s[j]){
            return false;
        }
        i++;
        j--;
    }
    return true;
};
