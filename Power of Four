/**
 * @param {number} n
 * @return {boolean}
 */
var isPowerOfFour = function(n) {
    if(n<=0){
        return false;
    }
    let powerOf2 = Math.log2(n)/2;
    return powerOf2 - Math.trunc(powerOf2) ===0 ? true: false;
};
